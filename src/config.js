// Replace these values with your Supabase project details.
// Create a free Supabase project and add an `expenses` table with columns:
// id (text primary key), category (text), amount (numeric), date (text), note (text).

export const SUPABASE_URL = 'https://kefziqivhdmhpjjsopbr.supabase.co';
export const SUPABASE_ANON_KEY = 'your-supabase-anon-key-here';
